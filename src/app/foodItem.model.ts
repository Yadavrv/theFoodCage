export interface FoodItem {
    userId: string;
  timestamp: Date;
  id?: string;
  totalPrice: string;
  name: string;
  description: string;
  price: string;
  imageUrl: string;
  quantity: string;
}